# 모델 폴더

데이터 구조, 도메인 개념, DTO, Entity, DB 테이블 관계를 정리하는 폴더입니다.

## 언제 보는가

- DTO나 VO를 만들 때
- DB 테이블과 Java 객체를 연결할 때
- Service, DAO 사이에 전달되는 데이터를 정리할 때
- 프론트엔드와 백엔드가 주고받는 JSON 구조를 정리할 때

## 문서 목록

```text
DATA_MODEL.md
DOMAIN_MODEL.md
```

